---
title: "Enumeration XRImageResolutionProfile"
sidebarTitle: "XRImageResolutionProfile"
description: "The XRImageResolutionProfile specifies which resolution a requested images should have. The available options are low resolution (LOW_RES) and native resolution (NATIVE)."
---

The XRImageResolutionProfile specifies which resolution a requested images should have.
The available options are low resolution (LOW\_RES) and native resolution (NATIVE).

## Enumeration Members

### LOW\_RES

LOW\_RES: "lowRes"

This will request images with a low resolution.

### NATIVE

NATIVE: "native"

This will request images with the native resolution.
